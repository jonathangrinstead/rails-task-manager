<br>
<br>
<h1>Task List</h1>
<br>
<br>
<ul>
<% @tasks.each do |task| %>
<li><%=link_to task.title, task_path(task), :class => "btn btn-success mb-3"%> <%=link_to "Edit", edit_path(task), :class => "btn btn-secondary mb-3" %><%=link_to "Delete",
  destroy_path(task), :class => "btn btn-danger mb-3", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
<% end %>
</ul>
<br>
<br>

<%= link_to "Create new task", tasks_new_path, :class => "btn btn-primary"  %>
